name: CI

on:
  # 迁移期只保留手动入口；所有消费者迁移完成后再统一开启 push/PR 验收。
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: docs-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  api:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: api
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: api/go.mod
          cache: true
          cache-dependency-path: api/go.sum
      - name: 校验模块依赖
        env:
          GOWORK: "off"
        run: |
          go mod tidy
          git diff --exit-code -- go.mod go.sum
      - name: 检查错误目录
        env:
          GOWORK: "off"
        run: |
          go run ./cmd/errorcatalog --check
          go run github.com/yueli-official/foundation/go/httpcontract/cmd/httpcontract@v0.4.1 \
            -errors contracts/http-result/error-catalog.json \
            -operations contracts/http-result/operations.json \
            -generate-go internal/docserr/catalog_gen.go -package docserr \
            -generate-ts ../web/app/generated/docsFailure.ts -ts-type DocsFailure \
            -generate-i18n ../web/app/generated/error-i18n-inventory.json -check
      - name: 检查 HTTP Result 合同覆盖
        env:
          GOWORK: "off"
        run: |
          go run ./cmd/httpoperations --check
          go run ./cmd/httpcontractcheck
      - name: 检查 HTTP Result 兼容性
        env:
          GOWORK: "off"
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          base_ref="${PR_BASE_SHA:-HEAD^}"
          if git cat-file -e "${base_ref}:api/contracts/http-result/error-catalog.json" && git cat-file -e "${base_ref}:api/contracts/http-result/operations.json"; then
            temp_dir="$(mktemp -d)"
            git show "${base_ref}:api/contracts/http-result/error-catalog.json" > "${temp_dir}/errors.json"
            git show "${base_ref}:api/contracts/http-result/operations.json" > "${temp_dir}/operations.json"
            go run github.com/yueli-official/foundation/go/httpcontract/cmd/httpcontract@v0.4.1 \
              -errors contracts/http-result/error-catalog.json \
              -operations contracts/http-result/operations.json \
              -base-errors "${temp_dir}/errors.json" \
              -base-operations "${temp_dir}/operations.json"
          fi
      - name: 检查 OpenAPI 契约
        env:
          GOWORK: "off"
          DOCS_OPENAPI_OUTPUT: ${{ runner.temp }}/docs-openapi.json
        run: |
          go run ./cmd/docs
          diff -u ../contracts/openapi/docs.json "$DOCS_OPENAPI_OUTPUT"
      - name: 运行测试
        env:
          GOWORK: "off"
        run: go test -timeout 60s ./...
      - name: 静态检查
        env:
          GOWORK: "off"
        run: go vet ./...
      - name: 构建命令
        env:
          GOWORK: "off"
        run: >-
          go build
          ./cmd/docs
          ./cmd/errorcatalog
          ./cmd/bootstrap
          ./cmd/bindingcheck
          ./cmd/healthcheck
          ./cmd/httpcontractcheck
          ./cmd/httpcontracts
          ./cmd/httpoperations

  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.28.2
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml
      - name: 安装依赖
        run: pnpm install --frozen-lockfile --ignore-scripts
      - name: 运行测试
        run: pnpm test
      - name: 类型检查
        run: pnpm typecheck
      - name: 生产构建
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: pnpm build

  container:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: api
            context: api
          - name: web
            context: web
    steps:
      - uses: actions/checkout@v4
      - name: 构建 ${{ matrix.name }} 容器
        run: docker build --tag yueli-docs-${{ matrix.name }}:ci ${{ matrix.context }}

  compose:
    runs-on: ubuntu-latest
    env:
      DOCS_POSTGRES_PASSWORD: docs-db-ci-secret
      DOCS_DATABASE_URL: postgres://docs:docs-db-ci-secret@docs-postgres:5432/docs?sslmode=disable
      DOCS_BOOTSTRAP_ADMIN_SUBS: '["11111111-1111-4111-8111-111111111111"]'
      DOCS_PUBLIC_ORIGIN: http://localhost:3003
      DOCS_WEB_SEAL_SECRET: docs-web-seal-ci-secret-1234567890
      DOCS_IDENTITY_ISSUER: http://localhost:8081
      DOCS_IDENTITY_INTERNAL_URL: http://identity:8081
      DOCS_IDENTITY_DISCOVERY_URL: https://identity-internal:8443/.well-known/openid-configuration
      DOCS_IDENTITY_JWKS_URL: https://identity-internal:8443/oauth2/jwks.json
      DOCS_ACCOUNT_ORIGIN: http://localhost:3000
      DOCS_OIDC_REDIRECT_URI: http://localhost:3003/auth/callback
      DOCS_OIDC_POST_LOGOUT_REDIRECT_URI: http://localhost:3003/
      DOCS_ASSET_INTERNAL_URL: http://asset:8082
      DOCS_ASSET_PUBLIC_ORIGIN: http://localhost:8082
      DOCS_ASSET_CLIENT_SECRET: docs-asset-ci-secret
      DOCS_ADMIN_SUB: 11111111-1111-4111-8111-111111111111
      DOCS_ADMIN_EMAIL: admin@example.test
      DOCS_ADMIN_PASSWORD: docs-admin-ci-password
      IDENTITY_POSTGRES_PASSWORD: identity-db-ci-secret
      IDENTITY_DATABASE_URL: postgres://identity:identity-db-ci-secret@identity-postgres:5432/identity?sslmode=disable
      IDENTITY_GLOBAL_SECRET: identity-global-ci-secret-1234567890
      ASSET_POSTGRES_PASSWORD: asset-db-ci-secret
      ASSET_DATABASE_URL: postgres://asset:asset-db-ci-secret@asset-postgres:5432/asset?sslmode=disable
      ASSET_UPLOAD_SECRET: asset-upload-ci-secret
      ASSET_STORAGE_CONFIG_SECRET: asset-storage-config-ci-secret
    steps:
      - uses: actions/checkout@v4
      - name: 校验三种 Compose 模型
        run: |
          docker compose -f compose.yaml config --quiet
          docker compose -f compose.hybrid.yaml config --quiet
          docker compose -f compose.attach.yaml config --quiet
