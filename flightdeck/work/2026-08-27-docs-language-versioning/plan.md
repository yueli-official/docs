# Plan

- [x] 1. 新增文档集语言注册、默认语言和逻辑文档身份约束迁移。
- [x] 2. 将版本领域改为“版本族 + 独立文档集 + 不可变 SemVer”，保留内部默认分区兼容旧链路。
- [x] 3. 实现文档集派生 API：复制语言、文档树、视觉元数据和逻辑文档键，派生文档默认转为草稿。
- [x] 4. 文档集后台使用三 Tab 管理基础、语言和关联版本，版本切换跳转目标文档集路径。
- [x] 5. 增加短文字或 Tabler 图标 Document Badge，并接入编辑器、目录、标题和管理列表。
- [x] 6. 支持单个文档集初始化语义版本，并从已有语言事务派生完整目标语言草稿树。
- [ ] 7. 在正常服务迁移窗口执行 0016，并完成文档集派生、语言派生、跨版本同页切换和 Badge 的真实浏览器验收。
