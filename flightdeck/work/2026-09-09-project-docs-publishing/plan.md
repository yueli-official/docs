# 执行计划与结果

交付：Docs 范围令牌、可分发文档发布 Skill、GitHub Release 文档源与后台自动更新。当前为本地版本。

## 已完成

- [x] 复用 Foundation PAT 在线验证与能力目录，开放文档读/创建/编辑/发布/下架和导入；治理、永久删除及来源凭证管理不向 PAT 开放。
- [x] 当前资源权限逐次检查；目录发现子集合能力不等于扩大访问范围。导入事务提交前重验令牌与权限。
- [x] 专用 BFF 上传/确认透传 PAT，专用 Asset 图片授权；确认批次并发保护、幂等结果、批次与文档同事务提交。
- [x] 修复根 index / 自定义 slug 重复导入匹配，按成功批次源路径更新；只同步本批次权限 scope。
- [x] `yueli-docs-publish` Skill、标准库打包/预检/发布/状态脚本、包规范和 GitHub Actions 模板；安装到本地 skills 目录。
- [x] GitHub 仓库 URL、Release docs.zip、目标文档集及版本、默认语言、upsert 或显式独占镜像。
- [x] 持久化绑定和运行记录、加密可撤销 PAT、15 分钟检查、立即检查/暂停/恢复/更换凭证/解除绑定。
- [x] 仅公开 GitHub 正式 Release，受限网络目标与重定向、大小/摘要检查、限流退避和可选公开 API 凭证。
- [x] 同附件/同摘要跳过、数据库并发锁、重启恢复、失败保留上次成功文档。
- [x] Go、真实隔离 PostgreSQL、脚本、HTTP 合同、Web typecheck、CLI Playwright 桌面/手机验收。细节与局限见 acceptance.md。
- [x] 接入知识、迁移说明、Skill 分发源与本地地址已保存。

## 落地选择

目标版本沿用 Docs 的“文档集发布版本”模型，选择器显示版本，不再增加一个重复版本字段。绑定即排队检查正式 Release，ZIP 预检无阻塞后自动导入，结果与报告在运行记录里展示。首版固定 15 分钟检查，GitHub 限流时按恢复时间延后；尚不提供任意仓库构建执行、私有仓库、预发布版本或通用外网 ZIP URL。

## 待真实项目联调

- [ ] 项目正式 Release 提供 docs.zip 后，完成真实外部附件的正向导入验收。当前 Keycrash Release 缺少附件；已生成打包工作流模板，但没有授权发布远程 Release。
- [ ] 用户本地体验后的反馈。

正式依赖制品、线上部署与远程 Release 发布未执行，不计作已交付。
